{
    "name": "OpenGL Development Container",
    "dockerFile": "Dockerfile",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "extensions": [
        "ms-vscode.cpptools"
    ],
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y cmake xorg-dev libgl1-mesa-dev libglu1-mesa-dev",
    "runArgs": [
        "--init",
        "--privileged"
    ],
    "remoteUser": "vscode"
}
